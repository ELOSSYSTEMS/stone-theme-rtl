# Archetype plugin-devkit (Shopify CLI plugin)

Repo:
- https://github.com/archetype-themes/plugin-devkit

Concepts to extract:
- Component workflow commands (build, explore, import maps)
- Locale sync tooling ideas
- Optional developer ergonomics, not required for Skeleton baseline

